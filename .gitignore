node_modules/
dist/
*.log
.env
.env.production
.env.local
.DS_Store
coverage/
.drizzle/
# local-only reference projects (quant-researcher / value-scope); anchored to
# repo root so it doesn't swallow the /data/legends dashboard route.
/legends/
