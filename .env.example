# --- Database (Neon serverless Postgres) ---
# Use the POOLED connection string for serverless.
DATABASE_URL=postgresql://user:password@host/db?sslmode=verify-full

# --- Anthropic ---
ANTHROPIC_API_KEY=sk-ant-...
# Note: the model names (signal=Opus, triage=Haiku) are code constants in
# packages/shared/src/config.ts — changing a model is a code change. Bump
# MODEL_CUTOFF below together with the signal model.
# Knowledge cutoff of the signal model — signals on events after this date are
# out-of-sample (look-ahead-safe).
MODEL_CUTOFF=2026-01-01

# --- Financial data (FMP) ---
FMP_API_KEY=...

# --- Discovery / universe selection ---
# Min |EPS surprise| (fraction) for the earnings scanner to flag a candidate.
SCAN_EARNINGS_SURPRISE_PCT=0.20
# TTL (days) before a discovery-promoted watchlist entry expires out.
DISCOVERY_TTL_DAYS=30

# --- Service-to-service URLs ---
# Local `pnpm dev` values below. docker-compose overrides these with the
# in-network service hostnames; Cloud Run uses the deployed service URLs.
ALPHA_URL=http://localhost:8082
PORTFOLIO_URL=http://localhost:8084
# web → data (candidates promote/dismiss from the dashboard).
DATA_URL=http://localhost:8081

# --- Per-service port (Cloud Run injects PORT) ---
# `pnpm dev` assigns each service its own port (8081/8082/8084); this is the fallback.
PORT=8080

# --- Observability ---
# DB log sink (system_logs, read by the dashboard) is on automatically except
# under test — no env knob. LOG_LEVEL / LOG_FORMAT below stay configurable.
# LOG_LEVEL=info        # debug|info|warn|error
# LOG_FORMAT=json       # machine-parseable lines for Cloud Run

# --- Dashboard (web) — keep these server-side, never NEXT_PUBLIC_* ---
DASHBOARD_PASSWORD=change-me
DASHBOARD_SESSION_SECRET=change-me-to-a-long-random-string
