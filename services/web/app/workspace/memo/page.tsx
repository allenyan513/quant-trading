import { MemoList } from "@/components/memo/memo-list";

/** Memo — top-level list of the user's investment memos + a minimal compose box. */
export default function MemoPage() {
  return <MemoList />;
}
