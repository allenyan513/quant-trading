{
  "name": "@qt/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=8081 tsx watch --env-file-if-exists=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "seed:watchlist": "tsx --env-file-if-exists=../../.env src/seed-watchlist.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@qt/shared": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.6.13",
    "pg": "^8.16.3",
    "tsx": "^4.19.2"
  }
}
