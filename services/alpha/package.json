{
  "name": "@qt/alpha",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=8082 tsx watch --env-file-if-exists=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@hono/node-server": "^1.13.7",
    "@qt/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.13",
    "pg": "^8.16.3",
    "tsx": "^4.19.2"
  }
}
